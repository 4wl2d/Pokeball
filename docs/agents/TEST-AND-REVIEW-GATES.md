# Test and Review Gates

> **Status:** derived noncanonical package. It neither defines nor extends Pokeball Core. Verify [BASELINE.md](BASELINE.md); Core prevails.

Testing follows applicability:

```text
base invariant tests
+ reachable path/risk tests
+ local policy-delta tests
+ evidence suites for claims actually made
```

Shared mechanisms and their evidence are tested once at exact revision/digest/scope. A Ball tests semantic wiring and its delta.

## 1. Routine change

Routine implementation does not create a full gate dossier. Before editing, resolve:

```text
exact baseline and affected authority
authoritative source and exact policy reference, if used
semantic delta
new, removed, and unchanged triggers
explicit exclusions
```

After editing, run base tests and the tests selected by changed triggers. Report the diff, policy delta, commands/results, and any unresolved triggered decision. Do not record absent paths individually, create `TriggerAbsenceProof` placeholders, or make a conformance or stronger guarantee claim. The proof is materialized only when a conformance/release claim or accepted ambiguity-resolution decision relies on absence.

## 2. Trigger-to-test matrix

| Trigger | Minimum evidence at the owning scope |
|---|---|
| every Ball | ownership/writer, permitted zone imports, pure deterministic Decision, exact state-profile-selected Snapshot/Event result and accepted frame without a runtime union or Event `nextState`, atomic acceptance, present-bound overflow; every physical helper import is Ball/local-role-owned or shared mechanical Foundation, with no ownerless shared domain/business utility or fifth semantic dependency kind; a Nucleus imports only exact declared target/producer-owned Application Surfaces required by its closed Query/Pulse/Decision contracts, with no ownership transfer or foreign State/internal/private-adapter access; every consumed/retained version is a named verified Context field or exact enclosing-binding proof; equal State/Pulse/valid per-Pulse Context/artifact yields the same candidate Decision under different remaining runtime capacity; Runtime/acceptor is mechanical and has no business-policy/result-selection/direct-State authority |
| numeric `maxTransitionSteps` selected instead of or in addition to a static proof | complete versioned `DecisionWorkMeter`; deterministic non-negative monotonic count; one zero start per `decide`; helper/phase/loop/retry/yield/representation-erasure no-reset cases; exact `N/N+1` acceptance; finite typed pre-acceptance/programming-fault overflow; cross-binding comparison only under equal meter identity/version, transition artifact version, and unit definition; static bound alone creates no meter artifact |
| output | complete batch, commit-before-dispatch, and fault behavior before and after acceptance |
| numeric `maxInputBytes`, `maxStateBytes`, or `maxOutputBytesPerDecision` without a static type-and-representation proof | one immutable dimension-specific `BoundedByteMeasure` with exact dimension/identity/version/representation/limit tuple; Input fixes raw-boundary or normalized-trusted stage plus exact metadata/Context inclusion and pre-`decide` `N/N+1`; State covers the complete candidate `nextState` semantic representation and pre-State/revision/output `N/N+1`; DecisionOutputs covers complete ordered sequence/envelope/correlation/`sourceOrdinal`/payload inclusion, later-mechanics exclusion, and whole-Decision `N/N+1`; every dimension proves exact alternate-representation mapping, erasure invariance, tuple incomparability, and no truncation or partial acceptance |
| synchronous causal completion | each deque/continuation item preserves its own trusted Pulse-to-Context association across order/yield/resume; root context is not inherited and runtime budget reaches admission only; pre-reserve total budget; at N+1 reject before acceptance or retain the accepted completion in one bounded `RetainedContinuation`; no Decision rewrite/drop/truncate/budget reset |
| cumulative fan-out exists | one accepted root operation/explicit causal scope; distinct accepted source-output/effective-route/consumer branch identity; tree, diamond, terminal, converging, co-reachable, and mutually exclusive counting; retry/redelivery versus new accepted tuple; async handoff versus independent root; static proof or existing total-causal-reservation enforcement; exact `N/N+1` with no partial State/revision/output/dispatch and no Context/protocol counter |
| detached/late result | stable source/provenance; duplicate, stale, and reordering cases only when reachable |
| retry | root proof of the original accepted `ReplyOutput(RequestAccepted(operationId))` frame with every Core §9.6 identity/lineage field preserved and only new `AttemptId`, plus pre-Intent `ValidationFailure(IdempotencyConflict)` mismatch; target ACK-before-result and exact accepted-result-frame replay with its Core §9.6 identity preserved and only new `AttemptId`; stable logical key; exactly one primary owner for each active failure mode; every other layer disabled or finite and proven semantically transparent; finite cumulative attempts; idempotency horizon only for duplicate-permitting irreversible execution |
| possible execution without proof | possible-send boundary, typed unknown, reconciliation/manual terminal policy |
| cancellation | reachable race orders and terminal conflict; Catalog `Searching|OutcomeUnknown` with present `pendingSearch`, exact three-ID operation/search-handle guard, exact `StaleSearchOperation(expectedOperationId, receivedOperationId)` Nucleus rejection/Interaction `DecisionRejected` carrier, stale mismatch with no Decision/State/revision/handle/projection/Effect/output, and other-state closed-rejection/no-borrowed-handle cases |
| timer | stable identity/late policy; generation only for replacement and dedup only for duplicate/redelivery risk |
| Query / `ReadDependency` | exact caller/target authority, one target-owned `Query -> ResultPayload` effective identity total over every reachable post-admission semantic outcome, target read/status authority, caller freshness/consistency requirement, and Assembly route; sparse version/stamp/actor/cache/order/buffer/retry/status fields; boundary failure before `read`; permitted, denied/redacted, deliberately non-disclosing when declared, wrong-provenance, and actor-independent cases; successful-evaluation purity and no Decision/accepted marker/revision/handle/output; no caller/Assembly/runtime payload invention, `BusinessRejection`, exception, post-admission `BoundaryResponse`, undeclared `NotFound`, payload/stamp/status re-export, or multi-source atomicity claim; no new Query-only mandatory limit field |
| status | one committed revisioned single-writer authority; a reserved root candidate before/after acceptance; root validation/admission/Decision rejection yields only `BoundaryResponse` and no operation/known row/marker/handle/output; namespace-and-materializer-barrier-proven `NotFound`; participant carrier remains an already accepted source Step facet; causal-order apply or bounded pending; idempotent duplicate and monotonic conflict behavior; lossless independent facets; pre-acceptance reservation/no eviction; covered-source/empty-pending marker before absence and no resurrection; target accepted-result crash/stop with source remaining Pending/Unknown |
| inter-Ball command | exact target-owned versioned mapping and static refusal class; accepted-source `ModuleCommandPulse`, target `decide`, accepted-target `ModuleResultOutput`, verified-source `ModuleResultPulse`; exact source/target fields and result-delivery tuple; no Assembly/generated synthesis; three carrier responses; accepted-result/carrier/version conflict; duplicate-before-result verified ACK only with no wait/redecision/revision/result/Resource action; duplicate-after-result exact accepted-frame replay with effective identity/`commandSource`/`resultSource`/target revision/`semanticHandle`/`sourceOrdinal`/payload preserved and only new `AttemptId`; command fingerprint/evidence conflict; command-vs-read; source level-1 reservation/failure, target level-2 reservation/failure for each carrier variant, pre-acceptance no-target-frame behavior, atomic level-1 carrier transfer, carrier-output reservation, successful `0/1/2`, and async budget preservation without inferred slot transfer |
| other inter-Ball edge | owner-authored `Application Surface`, target/producer-owned contract, caller Nucleus closed-contract need, sole Assembly edge, effective identity, finite graph/fan-out bounds; import transfers no ownership; reject foreign State/internals/private adapters, caller mirrors/redeclarations, protocol re-export, and Interaction/Assembly synthesis; inspect semantic ownership/import/sync-control/async-data/Assembly relations independently; independently acyclic compile-time-import and `Direct Control Dependency` graphs, including generated inline dispatch before handoff/yield; Ball-local/mechanical utility imports remain only ordinary compile-time edges absent a cross-authority condition |
| Flow/dispatcher | one resolved `FlowParticipation` per Flow/participant pair with exact authorities/Application Surface, non-empty bounded Flow-owned coordination set, and bounded refs to existing dependencies; four-kind dependency count retains referenced rows, rejects duplicate/equivalent-alias identities, accepts exact static `N`, and rejects the Ball contract at `N+1`; `maxRoutesPerFlow` counts only command/result round-trip mappings, treats command ingress/result return as one unit, assigns reads/signals/participation/references zero units, rejects exact duplicate/spelling-alias/equivalent-row/split-leg aliases, accepts exact static `N`, and rejects the Flow/Assembly contract at `N+1`; closed routes; coordinator only for stateful multi-authority workflow; compensation only when present |
| concurrent | mailbox/backpressure and finite workers; fairness only for starvation risk and out-of-order tests only when completion can reorder |
| durable | atomic selected snapshot/event transaction and recovery; flattened `AcceptedSnapshotDecisionFrame` versus `AcceptedEventCommit(decision: EventDecision)` with no Event `nextState`; output/value/ambiguity/fencing/status cases only when those paths exist |
| raw trust/representation edge | bounded parsing/validation/provenance quarantine for the exact edge; `ValidationFailure` only for representation or a declared type invariant independent of State/semantic Context/business policy; valid typed State/Context/business rules remain Nucleus-owned; promoting a fixed constraint changes the effective versioned protocol |
| actor context changes a Decision or Query/status-read authorization/result selection | trusted-boundary construction under Ball/Nucleus-owned schema; approved-issuer or fixed trusted same-stack issuer/realm proof; forged, tampered, stale, wrong-issuer/realm, and missing evidence fail closed; after valid read admission the pure Policy Gate selects one declared permitted/denied/redacted/non-disclosing variant from the total target-owned payload without another carrier; actor-independent Decision/read creates no actor artifact |
| logical-role or shared-foundation path | role map, permitted call graph, accepted-write sites, and cause/context/result provenance across Interaction, Nucleus, Resource/route, Assembly, Runtime/acceptor, status/read authority, and foundation, with Trusted Boundary verification/construction edge evidence separately inspectable from Interaction adaptation even when co-located; prove Runtime performs only validation handoff, admission/reservation, selected-frame publication, retained-output scheduling, and declared mechanical-observation routing; reject Runtime-owned business schema/policy/permission/read-result selection/retry choice, direct runtime state writes, Assembly-created meaning, mutable foundation business state/policy/route selection, globals, and service locators |
| external consequence or privileged action requiring both business and current technical authorization | every external Effect has minimum technical authority for the bounded operation class enforced at a real capability boundary plus a negative wrapper/admin-credential case; only the combined privileged-action trigger activates Policy Gate followed by current fail-closed Execution Gate, an actor-dependent read activates Policy Gate only, and Grant appears only for cross-authority action proof; for a present Grant, bind action-contract constrained target/payload fields only when defined and check the idempotency identity only when duplicate execution is possible; omit absent fields, checks, defaults, and placeholders |
| interpreter/secret/unsafe path | applicable parameterized, structured, capability-rooted, or context-encoded sink, including rooted-filesystem success/raw-traversal failure when present; exact-path/scope policy for every reachable State, output, persistence, serialization, log, and telemetry secret path plus a non-log negative; eight-field `WaiverRecord` tests only for an unsafe subtrigger |
| policy reference/delta | when referenced: digest/scope resolution, cycle/conflict/mismatch, override allowlist, effective bound, and proof that policy cannot suppress a trigger, weaken a law, or authorize a direct-control cycle |
| waiver | exact eight-field shape, pinned tests/evidence, review/remediation, literal conformance effect, and proof that the waiver is neither policy/precedent nor guardrail satisfaction |
| absence-dependent conformance/release claim or accepted ambiguity decision | exact `TriggerAbsenceProof`; present/absent path/risk/claim predicates, `always` misuse, present-claim contradiction, accepted/unaccepted ambiguity, wrong scope/profile/revision/digest, invalidation and reevaluation |
| claim | exact named boundary and scope, mechanism, assumptions, retention, evidence, and non-guarantees; reject any stronger downstream guarantee inferred only from source durability or retained pending work |

Mocks of framework objects inside the Nucleus do not replace transition tests. A declaration does not replace enforcement. Reused evidence is valid only for the exact artifact digest and scope named by the policy or claim.

## 3. Conformance or release review

Only an explicit conformance/release claim runs the full review workflow. Freeze Core, package, source, environment, evidence, and any project policy or Assembly in the claimed scope. Resolve the whole claimed scope. When the claim relies on absence of a non-`always` trigger, record the exact Core `TriggerAbsenceProof` once rather than creating empty guardrail artifacts. Verify its trigger class/anchor, exact scope/effective profile, one closed inventory-evidence choice, inventory revisions/digests, evaluated predicate, `Absent` conclusion, evidence owner, and invalidation conditions. A present trigger or claim, `always`, unaccepted ambiguity, wrong/stale/unresolved/conflicting evidence, or an invalidation condition blocks reliance until reevaluation.

Every release gate is `pass`, `fail`, or `partial`; `partial` is not a claim pass.

| Gate ID | Name | Question |
|---|---|---|
| `RG-01` | Scope | Are claimed scope, authority, policy references, goals/non-goals, extensions, every exact WaiverRecord plus its conformance effect, and every absence proof actually relied upon explicit? |
| `RG-02` | Protocol | Are every used protocol/read/output category, command ingress/result egress, refusal carrier, read dependency, and boundary identity closed without copied, hidden, open-string, or compatibility-alias variants; and is each `Query` bound to one target-owned payload total over all reachable post-admission outcomes without a non-owner-invented carrier? |
| `RG-03` | Authority | Does each mutable fact/workflow have one authority/writer, with retained references honestly classified? |
| `RG-04` | Decision | Are explicit per-Pulse inputs, every consumed/retained version as a named verified field or exact enclosing-binding proof, separately evidenced Trusted Boundary construction and Ball-owned interpretation, Pulse/Context association across Inline continuation, runtime-capacity/fan-out-reservation exclusion from `decide`, purity, the exact selected `SnapshotDecisionResult`/flattened `AcceptedSnapshotDecisionFrame` or `EventDecisionResult`/`AcceptedEventCommit` contract without Event `nextState`, present bounds including every triggered dimension-specific byte measure and the full work-meter contract, atomic acceptance, output ordering, same-state accepted result, alternative-completion transfer, and exact validation-versus-State/Context business-stage behavior proven? |
| `RG-05` | Async | Are all reachable root Reply replay/pre-Intent conflict, command ACK-before-result/exact-result replay, provenance, identity, carrier/result/refusal meaning, retry, unknown/manual terminal, cancellation, delivery, and lossless status/materializer races total, including accepted-only root `OperationId`/known status, namespace-proven absence, and downstream target nonacceptance only as an already accepted source facet? |
| `RG-06` | Composition | Is every physical helper import classified as one Ball/logical-role-local utility or shared mechanical Foundation, with ownerless shared domain/business semantics rejected or assigned to a Ball/Flow Application Surface/protocol and no fifth dependency kind? Are all existing read/command/signal/Flow edges, exact four-kind dependency and effective Flow round-trip-route counts, exact target/producer-owned Application Surfaces imported only for closed caller Nucleus contracts, one-per-pair resolved FlowParticipations, independent ownership/import/sync-control/async-data/Assembly relations, effective protocol identities, triggered versions, Flow ownership, graph ceilings, independently acyclic import/direct-control graphs, and post-handoff asynchronous-feedback owner/identity/budget/escape/fan-out conditions resolved with no ownership transfer, foreign internals, caller mirror/redeclaration, re-export, or Interaction/Assembly synthesis? |
| `RG-07` | Security | Are every reachable trust, actor-origin for Decision and read semantics, protocol/type validation versus State/Context business-stage ownership, total target-owned actor-dependent read result and Policy-Gate selection, Policy/Execution Gate, bounded-operation real capability boundary, privilege, applicable safe-sink form, exact-path/scope secret flow, foundation, and unsafe path closed under its own trigger? |
| `RG-08` | Profiles | Does each effective profile and claimed guarantee name the exact boundary/scope, mechanism, assumptions, retention, evidence, and non-guarantees without inferring a stronger downstream guarantee from source durability or retained pending work? |
| `RG-09` | Limits | Does every present variable dimension resolve to one finite enforced bound without unauthorized delta; does each numeric input/State/output byte limit have either a static type-and-representation proof or one exact dimension-specific `BoundedByteMeasure`, with the selected input stage and metadata/Context inclusion, complete candidate-State semantic representation, complete ordered-output inclusion/exclusion, exact alternate-representation mapping, erasure invariance, mismatched-tuple incomparability, dimension-specific `N/N+1`, and no truncation or partial acceptance; do dependency counts use the exact four declaration kinds and static contract-resolution `N/N+1`, and does the Flow-route count include only command/result round-trip mappings while reads/signals/participation/references consume zero units, with exact duplicate/alias rules and static Flow/Assembly rejection at `N+1`; does each triggered Decision Work Meter resolve meter identity/version, transition artifact version, unit/cap/overflow with deterministic monotonic no-reset `N/N+1` behavior; and does each present `maxCumulativeFanout` use one exact causal scope/branch identity, complete aggregation/duplicate/handoff rules, and static proof or exact no-partial runtime `N/N+1` admission? |
| `RG-10` | Integrity | Do all 44 Core-embedded source records, complete §20 audit entries, limited §20.1 applicability/ownership/navigation rows, explicit primary §17 test routes, policy refs, examples, tests, indexes, traceability, package integrity, bilingual public projections, and claim/absence evidence agree without orphan modal wording; and does exhaustive semantic review cover each unique authority/test/checklist route once while treating generated §§20/22 alternatives as mechanically equality-checked views rather than duplicate semantic reading? |

## 4. Profile suites

Run only suites selected by the effective profile or claim.

### Inline / Transient

- canonical `decide(...) -> SnapshotDecisionResult<State>` and flattened `AcceptedSnapshotDecisionFrame<State> { commitRevision?, nextState, outputs }`, acceptance-publication faults, and complete in-process retained frame when outputs exist; no Event branch is required;
- exact N/N+1 causal-budget boundary when synchronous completion can cause another Decision, including per-item trusted Pulse/Context association, root-context non-inheritance, runtime-budget-only admission, and `RetainedContinuation` fate;
- same-stack command source level-1 alternative-completion reservation, accepted-target level-2 result reservation, each carrier variant's pre-acceptance no-target-frame/atomic-transfer path, carrier-handling output reservation, successful levels `0/1/2`, one source-to-target direct edge, result-return non-edge, and `CausalBudgetExceeded` through the carrier;
- no hidden queue/thread/serialization tax;
- explicit process-loss boundary when external ambiguity can exist; and
- benchmark only when making a performance claim.

### BoundedConcurrent

- mailbox full/backpressure and finite workers; fairness only when separately queued inputs could starve;
- out-of-order input/result, plus duplicates only when duplicate delivery is possible;
- cancellation/deadline/result races only when those paths are reachable; and
- finite causal/fan-out budgets only for present causal or routed dimensions, with exact source-output/route/consumer counting and async handoff preserving causal scope/depth/budget; exact `N/N+1` accepts no partial Decision.

### SnapshotOutbox / EventJournal

- selected mode-specific frame: flattened `AcceptedSnapshotDecisionFrame<State>` for SnapshotOutbox or `AcceptedEventCommit { ..., decision: EventDecision }` for EventJournal, with State reconstructed through `evolve` and no independent Event `nextState`; before/inside/after-transaction crashes; unknown commit only when ambiguity is possible;
- recovery from accepted records without rerunning current transition code;
- redelivery, ACK loss, attempt exhaustion, status, retention, and fencing only when reachable; and
- accepted target result survival across crash-before-dispatch, target tuple-keyed `DispatchStopped`, and source Pending/Unknown until verified result/reconciliation; and
- exact retained cross-Decision values and migrations only when later work uses them.

### Hardened / Isolated

- actor-context authenticity/integrity and wrong/unapproved issuer/realm evidence whenever actor context can change a Decision or Query/status-read authorization/result selection, independently of privilege;
- audience/payload/expiry/revocation and current action proof only for the corresponding privileged controls;
- minimum technical authority for the bounded operation class at a real capability boundary, including rejection of a wrapper/admin-credential substitute;
- applicable parameterized, structured, capability-rooted, or context-encoded safe-sink behavior only for an interpreter/dialect edge;
- exact-path/scope policy across every reachable State, output, persistence, serialization, log, and telemetry secret path; and
- an ordinary scoped credential remains a valid trusted `InProcess + Hardened` negative fixture when no hostile-containment or separate-principal requirement exists; and
- isolated credential separation plus crash/escape/resource caps when containment is required, selected, or claimed.

## 5. Agent Pack gates

These gates validate the package itself. They do not certify a consuming project.

| Gate ID | Name | Pass condition |
|---|---|---|
| `AP-GATE-01` | Package closure | Every declared file exists and every relative link resolves. |
| `AP-GATE-02` | Snapshot integrity metadata | The candidate Core version/status/hash/bytes and package file count/digest match `BASELINE.md`; the Core hash occurs only there, and no internal review verdict is exported as package authority. Consumer readiness additionally requires that these exact bytes come from one immutable published snapshot. |
| `AP-GATE-03` | Noncanonical precedence | Every file is marked derived; no guidance overrides Core or exact policy precedence; every repeated projection modal names and remains set-equal to its marked body source clause, and an orphan `MUST`/`MUST NOT` scan is empty. |
| `AP-GATE-04` | Rule integrity | Exactly 37 `PKB-AR-*` definitions are unique and defined only in `AGENT-CONTRACT.md`: the prior 35 IDs in order plus only `PKB-AR-GOV-005` and `PKB-AR-PRT-004`; contract and traceability sets are equal. |
| `AP-GATE-05` | Core coverage | §§0–23 and all 13 Core §8.12 rows are indexed set- and order-equal by concern and anchor; the 44-row reference block explicitly resolves each Core-embedded source record to its complete §20 audit entry, limited §20.1 applicability/ownership/navigation row, and primary §17 test route, with exact unique ordered `PBA-01–44` sets in every projection; all 94 Core glossary terms are indexed; ordered union checks also reject bare `ModuleResult`, top-level `TimerFired`, `SignalOutput`, and compatibility aliases. |
| `AP-GATE-06` | Trigger traceability | Every agent rule has a Core source and complete source tuple: modal, class/exact trigger, declaration/semantic owner, scope, enforcement/failure behavior, reuse, absent-trigger behavior, and gate. Anchor equality alone does not pass. Exact §6.13 failure-stage, total admitted-read result, and closed-admission-reason mappings are traced. |
| `AP-GATE-07` | Policy and waiver closure | The compact project-policy template contains exact metadata/source/policy resolution, permits only local deltas, omits Ball inventories/empty optional sections, and is statically resolvable; any waiver has exactly the eight Core fields and neither weakens nor suppresses a guardrail. |
| `AP-GATE-08` | Applicability and fixture closure | The complete H fixture catalog below passes: trigger absence, explicit consumed/retained version fields, exact selected Snapshot/Event result and accepted frames, actor/context/Dual Gate/foundation and distinct Trusted Boundary edge, Runtime/acceptor non-authority, protocol/type-validation versus State/Context business-stage fixtures, Ball-local/shared-mechanical/ownerless-domain utility classification, exact dependency/Flow-route and cumulative-fan-out counting/admission, every active numeric input/State/output `BoundedByteMeasure` with its exact stage/value scope and dimension-specific `N/N+1`, accepted-only root identity/status plus original Reply replay/pre-Intent conflict, positive constrained-plus-duplicate and negative Grant-subtrigger-absent fixtures, total target-owned read result, Nucleus/Application-Surface ownership and negative mirror/synthesis fixtures, FlowParticipation, command/result/refusal, ACK-before-result/exact-result replay and alternative completion, bounded-operation real capability, four safe-sink forms, complete secret paths, qualified claims, Decision Work Meter, causal/status materializer, Checkout `StillUnknown` manual terminal and Catalog domain-revision/stale-cancel guards, failure stage, admission reason, `Draining`, pilot measures, and negative-adoption cases. Inferred triggers cannot be suppressed and closed absent paths add no artifacts. |
| `AP-GATE-09` | Claim boundary | Routine work requires no evidence dossier or absence-proof placeholder; every explicit claim contains its exact named boundary and scope, mechanism, assumptions, retention, evidence, non-guarantees, applicable waiver effects, and every relied-upon exact `TriggerAbsenceProof`, and claims no stronger downstream guarantee from source durability or retained pending work alone. Present/`always` contradictions or any `MUST`/`MUST NOT` violation block conformance in their exact scope. |
| `AP-GATE-10` | Governance-free portability | A clean standalone target with no source-repository governance files resolves a sparse singleton, triggered local Balls, and one shared declaration for two covered Balls plus an allowed delta; wrong/stale/cyclic/conflicting/version/profile/environment refs fail. The overlay is optional, no Query-specific limit field is invented, and package licensing, links, hashes, and target software license remain intact. |

The complete revision fixture catalog is additive; it does not replace the Core acceptance set:

- **Source authority and closure:** semantically read every unique authoritative source record and applicable §17/§18 route once; verify one mechanically equal complete §20 audit entry, limited §20.1 index row, and primary §17 route for each `PBA-01–44`, and mechanically equality-check generated §22 term projections instead of semantically rereading those alternatives. Compare all eight source/§20 fields and every affected package modal, trigger, owner, scope, enforcement/failure, reuse/absence behavior, and route; validate §20.1 only against its declared columns; reject orphan projection `MUST`/`MUST NOT`; make no claim that exhaustive unique-source review is trivial. Verify exact ordered `Pulse` and `SemanticOutput` unions and negative membership for bare `ModuleResult`, top-level `TimerFired`, `SignalOutput`, and every compatibility alias. Verify representation/closed-type `ValidationFailure`, valid typed State/Context mutation `BusinessRejection`, and admitted-read payload-result stages, including the protocol-version change required to promote a fixed constraint. Verify validation/admission/root pre-acceptance Decision rejection creates only `BoundaryResponse` and no accepted operation/status/handle/output; separately verify accepted target result, downstream target pre-acceptance carrier as an accepted source Step facet, post-acceptance Resource failure/timeout/unknown, delivery stop, and programming-fault carriers/status without later-history rewrite. Every fallible concrete profile exposes a finite closed `AdmissionFailure.reason` union, rejects unknown/open strings, and a nonfallible path exposes no empty union.
- **Command/result bridge:** verify every `ModuleCommandPulse`, `ModuleResultOutput`, and `ModuleResultPulse` field, issuer provenance, target-frame `sourceOrdinal`, correlation-only `semanticHandle`, and delivery key `(effectiveProtocolIdentity, commandSource, resultSource)`, with no replacement identity type. Cover routed/same-stack success, all three pre-acceptance `BoundaryResponse` choices, forged/tampered/missing/wrong provenance, accepted same-state refusal through flattened `AcceptedSnapshotDecisionFrame` or `AcceptedEventCommit(NoDomainChange)` without Event `nextState`, duplicate replay without another Decision/revision, carrier/result conflict, dynamic reclassification and wrong version, output/byte and conditional target status bounds, crash before result dispatch, target `DispatchStopped`, late result, handle collision, ACK/result orderings, and exact-one/live-route sparsity. Same-stack covers source inability to reserve level 1; level 1 available but level 2 unavailable for every carrier variant; no accepted target level/frame/revision/output; atomic transfer of level 1 to source `decide(ControlPulse)`; committed `RejectedBeforeAcceptance + NotExpected`; further carrier-handling synchronous output reservation; successful levels `0/1/2`; result return as no reverse edge; and async preservation of causal scope/depth/budget without inferring the same-stack transfer rule.
- **Checkout command/refusal projection:** cover all nine target mappings. The initial call supplies verified `DecisionContext(actorContext=A, artifactVersion=IV)`, retains exactly `interactionArtifactVersion=IV`, distinguishes it from `transitionArtifactVersion`, rejects missing/stale/mismatched/untrusted versions before acceptance, preserves accepted `IV` on retry, and uses it for recovery/migration/quarantine. A reserved root candidate followed by validation/admission/Checkout-Decision rejection creates no root operation/status/handle/output/marker; after acceptance the same ID selects one known operation. `InventoryReservationRejected` is never a carrier; `DefinitelyNotCaptured(RejectedBeforeAcceptance)` describes the original Capture; definitive Order refusal is an accepted result; Flow rejection copies no carrier reason. Carrier maps to `RejectedBeforeAcceptance + NotExpected`, accepted rejection to `Accepted + Rejected`, and carrier `BusinessRejection` is informational only.
- **Reads, composition, actor context, gates, and foundation:** verify complete `ReadDependency` resolution, triggered-only fields, boundary failure before `read`, and one successfully evaluated target-owned `ReadResult` payload total over every reachable post-admission outcome. Exercise permitted, denied/redacted, deliberately non-disclosing when declared, wrong-provenance, and actor-independent cases; reject caller/Assembly/runtime payload invention, `BusinessRejection`, exception, post-admission `BoundaryResponse`, undeclared `NotFound`, any Decision/accepted marker/revision/handle/output, target payload/stamp/status synthesis, atomic multi-source promise, or new Query-only limit field. Classify each physical helper import as Ball-local to exactly one logical role or shared mechanical Foundation; keep it in the acyclic compile-time graph, create no fifth semantic dependency/route, reject ownerless shared domain/business semantics and Foundation relabelling, and pass both explicit Ball-local-copy and one Ball/Flow-owned Application-Surface alternatives. Verify each target/producer-owned Application Surface is imported by a caller Nucleus only when required by a closed Query/Pulse/Decision contract; import transfers no ownership and rejects foreign State/internals/private adapters, caller redeclaration or a structurally identical mirror type, protocol re-export, and Interaction/Assembly synthesis. Verify one resolved FlowParticipation per pair with a non-empty bounded coordination set and bounded refs to existing dependencies; inspect semantic-ownership, import, sync-control, async-data, and Assembly relations separately, with each Application Surface import present in both compile-time and `Direct Control Dependency` graphs. Checkout imports exactly the four Cart/Inventory/Payment/Order-owned surfaces and resolves nine target-owned command/result mappings across nine routes; Auth and CheckoutFlow are not participants. Compare `Payment.GetOperationStatus` command semantics with an ordinary status Query. Cover every consumed/retained version as a named verified Context field or exact binding proof, actor-dependent Decision and read paths, actor-independent sparsity, fixed trusted same-stack issuer/realm proof, forged/tampered/missing/stale/wrong issuer/realm/target/field/version, and the Payment path from accepted command through action-scoped context/grant, Nucleus business result, and immediate Execution Gate. For the Grant subtrace, one action-contract-constrained, duplicate-capable path proves matching constrained-field and idempotency-identity binding and rejects substitution/mismatch; an otherwise equivalent Grant with neither subtrigger requires or materializes no corresponding field, check, default, or placeholder while retaining issuer, actor, audience, action, object, operation, and validity checks. The seven logical roles plus separately inspectable Trusted Boundary edge map/call graph/provenance proof proves Runtime/acceptor only handles admission/reservation, selected-frame publication, retained-output scheduling, and declared mechanical-observation routing; it rejects Runtime-owned business policy/permission/read-result selection/retry choice, direct runtime writes, Assembly-created meaning, mutable foundation business communication/policy/route selection, hidden globals, and service locators.
- **Capability, sinks, secrets, and claims:** prove a positive bounded-operation capability enforced at a real restricted boundary and reject an unrestricted administrator credential plus local wrapper/check. Exercise every applicable parameterized, structured, capability-rooted, and context-encoded safe-sink form, including rooted filesystem success and raw traversal failure when present. For each secret, cover every reachable State, output, persistence, serialization, log, and telemetry path under an exact path/scope policy and fail a non-log leak. A claim fixture names its exact boundary/scope, mechanism, assumptions, retention, evidence, and non-guarantees and rejects every stronger downstream inference based only on source durability or retained pending work.
- **Status materialization:** generic fixtures cover reserved root candidate before/after acceptance; root validation/admission/Decision rejection producing only `BoundaryResponse` and no accepted operation/known row/marker/handle/output; namespace-and-barrier-proven `NotFound`; participant carrier isolation; observation-before-source, causal apply/bounded pending, duplicate and non-equivalent conflict, every reachable independent facet, pre-acceptance `N/N+1`, no eviction/truncation, covered-source/empty-pending retention marker, marker-before-absence, expiry, invalidation/conflict, late duplicate no-resurrection, and accepted-target-result crash/stop with source Pending/Unknown. Checkout covers the fixed canonical ten-handle order `cartLock`, `initialAcceptanceReply`, `inventoryReservation`, `paymentCapture`, `paymentCancellation`, `paymentReconciliation`, `orderConfirmation`, `inventoryRelease`, `paymentRefund`, `cartUnlock`; reply-only, reply-plus-nine, both/permuted arrivals, duplicate/conflict, reason/attempt/last-observation retention, reservation/no eviction, and capacity `10/11`. Catalog keeps its Ball-owned domain `revision` distinct from acceptor-owned `CommitRevision` unless an explicit mapping proves equality and preserves both meanings; it covers all 6/6 state-to-view mappings and `ProductSelected` transitions, the total lifecycle × cancellation × unknown × rejection-reason model and compatible late orderings, one ordinal-0 Signal, exact `2.0.0/2.0.0` route, actor-independent reserved ID, and authoritative v1 upcast or quarantine without invented reason.
- **Limits, boundaries, and adoption:** for every active numeric input/State/output byte limit not closed statically, verify one immutable dimension/identity/version/representation/limit tuple, exact input stage and metadata/Context inclusion, complete candidate-State semantics, complete ordered-output semantics, alternate-representation mapping, erasure invariance, tuple incomparability, and dimension-specific `N/N+1` with no truncation or partial acceptance. When a numeric `maxTransitionSteps` declaration exists, verify the full meter identity/version, transition artifact version, unit/cap/overflow view, deterministic non-negative monotonic counting, one zero start per `decide`, helper/phase/loop/retry/yield/representation-erasure no-reset cases, exact `N/N+1`, and cross-binding comparability only under complete identity equality; a static bound alone creates no meter artifact. When `maxCumulativeFanout` exists, verify one accepted root/explicit causal scope and distinct accepted source-output/effective-route/consumer branch identity: tree, diamond, terminal, converging and co-reachable traversals count; mutually exclusive alternatives share their maximum reservation; duplicate/redelivery of the same tuple/route does not increment while a new accepted tuple does; async handoff preserves scope while an independent root starts fresh; static proof creates no counter, otherwise exact `N/N+1` rejects the whole over-limit Decision without partial State/revision/output/dispatch and never enters Context. Run the §4 decision tree and concrete combine/separate/Ball-local utility/shared mechanical Foundation/ownerless shared domain utility/Feature/Flow/Read Model falsifiers; verify Core constrains valid authority graphs without promising one unique decomposition. Prove that decision-relevant UI/transport values are committed State or explicit trusted current `Pulse`/`DecisionContext`, while only non-decision mechanics are `EphemeralState`. Distinguish material coordination—owned lifecycle, ordering/branch/join, compensation/recovery/cancellation, reconciliation, or independent terminal outcome—from call count or one hop. The project-owned worksheet is `SHOULD` guidance only during active adoption/pilot work; its project-selected methods/ranges for state size, per-key load, transition cost, and design-time tax/benefit drive continue/reshape/stop without universal thresholds. Negative-adoption fixtures cover a stateless library/value/algorithm, presentation-only mechanical state, a passive adapter, refusal to establish one writer/authority/closed bounded contract, and pilot cost exceeding benefit. The minimal task route follows §§0.1–0.2, §§3–5, §§6–8, only triggered §§9–13, conditional §14, examples §§15–16, and checks §§17–18; a full semantic audit additionally reads all unique authority once and mechanically equality-checks the alternative generated §20/§22 views and limited §20.1 index, without claiming exhaustive unique-content review is trivial.
- **Absence, applicability, lifecycle, and portability:** cover present and absent path/risk/claim predicates, `always` misuse, present-claim contradiction, accepted/unaccepted ambiguity, wrong scope/profile/revision/digest, every invalidation condition and reevaluation, plus actor-dependent/independent Query cases. Routine and negative-adoption work creates no proof/worksheet placeholder. `Draining` rejects new mutations, admits available Query/status reads with the successfully evaluated total target-owned result—including any declared denied/redacted/non-disclosing variant—rejects an unavailable read before `read`, and continues declared accepted-operation inputs. A clean relocated package with no governance files proves the sparse singleton, one shared declaration for two Balls, allowed delta, and stale/cyclic/conflicting/wrong-version/profile/environment rejection; the optional overlay and licensing baselines remain intact.

## 6. Claim record

```text
Review/claim ID and owner
Exact Core/package/source/environment baselines plus project policy/Assembly when in scope
Claimed scope and exact wording
Exact named guarantee boundary
Resolved policy references/deltas, exact WaiverRecords and conformance effects if any, trigger inventory, and every exact TriggerAbsenceProof actually relied upon
Questions, coverage units, previous evidence, exclusions
Applicable rules and RG-01..10 results
Mechanisms, assumptions, retention, evidence artifacts, observed results
For any cross-binding numeric maxTransitionSteps comparison: equal meterIdentity, meterVersion, transitionArtifactVersion, and unitDefinition; otherwise separate incomparable observations
Explicit non-guarantees
Proof that source durability or retained pending work alone establishes no stronger downstream guarantee
Failed/partial units and follow-up trigger
```

Valid final wording is bounded: “On exact baseline X and claimed scope Y, gates A–J passed; no failed or partial unit remains in that scope.” Do not say “perfect,” “secure in general,” “production-ready,” or use an unqualified delivery/once-only claim.
