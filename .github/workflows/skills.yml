name: Skills

on:
  pull_request:
  push:
    branches: [master]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test compact skills and local composition behavior
        run: python3 -m unittest discover -s tests -p 'test_*.py' -v
      - name: Check compact skills and source integrity
        run: python3 scripts/check_skills.py
